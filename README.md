Disarankan menggunakan virtual environment biar gak tabrakan, dan kalau pake notebook lokal.

Kalau pake Google Colab, mangga diskip

```
python -m venv Env
./Env/Scripts/activate
pip install -r requirements.txt
```